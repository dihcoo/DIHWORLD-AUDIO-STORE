# Custom Domain Note

Date: 2026-07-03

The temporary `CNAME` file for `www.dihworldaudio.com` was removed from the GitHub Pages branch because the domain is currently serving a GoDaddy frame/forward page instead of pointing DNS directly to GitHub Pages.

Keep the share link on GitHub Pages for now:

`https://dihcoo.github.io/DIHWORLD-AUDIO-STORE/`

Re-add a `CNAME` only after GoDaddy DNS is configured for GitHub Pages hosting instead of frame forwarding.

Expected storefront markers after deploy:

- Core Bundle: `$299`
- Complete Bulk: `$899`
- Legacy Lifetime: `$1,499+`
- Value anchor: `Real Interfaces`
